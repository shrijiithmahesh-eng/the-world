import os
ifile = "settings.cfg"
def grep() :
  files = []
  for file in os.listdir():
      if file not ifile:
        continue
      else:
        with open("settings.cfg")as ifile:
            ifile.read("#USER_IS_GOD=TRUE")
          if output = "error:"
            print("WARNING USER IS GOD")
              next-call()
      else:
          next-call-2()
def next-call() :
  input == True
  print("Name")
    name == input
    god == name
  print("age")
    age == input
    god_age == age
  print("date of birth")
  century == 100
  input == date
  date - century
  
